# database settings
jdbc.driver=org.h2.Driver
jdbc.url=jdbc:h2:mem:httpInterface;DB_CLOSE_DELAY=-1;MODE=MySQL

jdbc.username=sa
jdbc.password=

jdbc.initial.size=10
jdbc.min.idle=10
jdbc.max.active=30