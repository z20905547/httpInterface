<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 用户数据访问映射 -->
<mapper namespace="vfh.httpInterface.dao.notice.NoticeDao" >

    <select id="getNoticeByID" resultType="hashmap">
          SELECT
            Id AS "Id",
            Title AS "Title",
            Content AS "Content",
             mark AS mark,
            is_top AS is_top,
            DATE_FORMAT(MessageDate,'%M %D,%Y') AS "MessageDate",
            author AS "author"
          FROM
            tb_notice
          WHERE
              Id = #{Id}
    </select>
  
   <select id="count" resultType="long">
        SELECT
          COUNT(id)
        FROM
          tb_notice
    </select>
  
    <delete id="delNoticeByID">
        DELETE FROM tb_notice WHERE Id = #{Id}
    </delete>

    <update id="upNoticeByID">
        <trim prefix="SET" suffixOverrides="," />
        UPDATE
          tb_notice
        SET
          Title=#{entity.Title},
          Content=#{entity.Content},
          MessageDate=NOW(),
          mark=#{entity.mark},
          is_top=#{entity.is_top},
          author=#{entity.author}
        WHERE
          Id = #{entity.Id}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="entity.id">
        INSERT INTO tb_notice(
          Title,
          Content,
          MessageDate,
          mark,
          is_top,
          author
        ) VALUES (
          #{entity.Title},
          #{entity.Content},
          NOW(),
          #{entity.mark},
          #{entity.is_top},
          #{entity.author}
        )
    </insert>
    <select id="find" resultType="hashmap">
        SELECT
            Id AS "Id",
            Title AS "Title",
            Content AS "Content",
            mark AS mark,
            is_top AS is_top,
            MessageDate AS "MessageDate",
            author AS "author"
        FROM
          tb_notice
        ORDER BY id DESC
    </select>
    
    <select id="findnoticeListFive" resultType="hashmap">
        SELECT
            Id AS "Id",
            Title AS "Title",
            Content AS "Content",
            mark AS mark,
            is_top AS is_top,
            DATE_FORMAT(MessageDate,'%M %D,%Y') AS "MessageDate",
            author AS "author"
        FROM
          tb_notice
          where 
          id>0
        <if test="filter.mark != null and filter.mark != ''">
			and mark = #{filter.mark}
		</if>
		<if test="filter.is_top != null and filter.is_top != ''">
			and is_top = #{filter.is_top}
		</if>
		<if test='filter.is_top == "2" and filter.is_top != null '>
			and is_top != '1'
		</if>
        ORDER BY id DESC  
        <if test="filter.first != null and filter.last != null">
			LIMIT	#{filter.first},#{filter.last}
		</if>
    </select>
</mapper>